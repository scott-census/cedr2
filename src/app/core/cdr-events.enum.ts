export enum CdrEvents {
  system,
  add_states,
  button_submit,
  do_update,
  grid_init,
  init,
  layout_change,
  map_init,
  resize,
  select_geo_selector_option,
  select_year_selector_option,
  selected_names_updated,
  selection_selected,
  set_geo_selector_options,
  set_year_selector_options,
  trends_init
}
